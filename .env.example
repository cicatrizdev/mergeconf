# Sem estas variáveis a API roda com dados em memória (espelho de supabase/seed.sql) — zero setup.
# Com elas preenchidas, a API usa o Supabase de verdade (necessário a partir do Módulo 4).
SUPABASE_URL=
SUPABASE_ANON_KEY=
